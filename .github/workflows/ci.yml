name: CI

on:
  push:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  ci:
    uses: Stakely/ci-cd-orchestrator/.github/workflows/frontend.yml@main
    with:
      dockerfile: Dockerfile
      manifest-key: frontend.docs
      manifests-repo-slug: kubernetes-manifests
    secrets: inherit
